import GlobalStyle from '../../styles/global';

import AppRouter from './routes/AppRouter';

const Core = () => {
  return (
    <>
      <GlobalStyle />
      <AppRouter />
    </>
  );
};

export default Core;
